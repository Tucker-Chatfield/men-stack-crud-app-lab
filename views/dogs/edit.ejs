<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <a href="/dogs">Back to Dogs</a>
    <h1>Edit <%= dog.name %></h1>
    <form action="/dogs/<%= dog._id %>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" />

      <label for="likesToPlay">Like to play?</label>
      <input type="checkbox" name="likesToPlay" id="likesToPlay" <% if (dog.likesToPlay) { %>checked<% } %> />

      <button type="submit">Add Dog</button>
</body>
</html>